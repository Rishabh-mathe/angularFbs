import { ReplaceContantDirective } from './replace-contant.directive';

describe('ReplaceContantDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceContantDirective();
    expect(directive).toBeTruthy();
  });
});
